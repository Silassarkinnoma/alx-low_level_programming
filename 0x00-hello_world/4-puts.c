#include <stdio.h>

/**
 * main - entry point
 *
 * Return: return 0 if successful or 1 if failed
 */
int main(void)
{
     puts("\"Programming is like building a multilingual puzzle");
     return (0);
}
